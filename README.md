# JavaScript-Analog-Digital-Clock
A simple and clean analog + digital clock built using HTML, CSS, and JavaScript. It updates every second using setInterval and rotates the clock hands using CSS transform.
